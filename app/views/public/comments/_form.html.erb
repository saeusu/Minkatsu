<%= form_with(model: [post, Comment.new], local: false) do |form| %>
   <%= form.text_area :comment, rows:'5',placeholder: "コメントをここに", class: "w-100 comment-textarea" %>
   <%= form.submit '送信', class:"btn btn-success" %>
<% end %>