<div class="container">
  <div class="row">
    <div class="col">
      <h4 class="underline mt-2">ジャンル編集</h4>
      <br>
        <%= form_with(model: [:admin, @genre]) do |f| %>
          <div>
            ジャンル名　　<%= f.text_field :name %>
              <%= f.submit '変更を保存', class: "btn btn-success" %>
            <% end %>
          </div>
    </div>
  </div>
</div>